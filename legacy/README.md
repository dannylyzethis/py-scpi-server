# Legacy implementations

These scripts are retained for behavior comparison while the packaged emulator is developed.

- `server-py.py` is the former unversioned implementation.
- `server-py-ver2.2.py` is the former v2.2 implementation.

Neither file is a supported entry point. Use `scpi-emulator` or `python -m scpi_emulator`.
